{
  "name": "striangle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trading-inspired charting and strategy backtesting PWA",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.mjs && node --check dist/engine.mjs && node --check dist/sw.js && node --check dist/market-data.mjs && node --check dist/optimizer.mjs && node --check dist/optimizer-worker.mjs && node --check dist/bots.mjs && node --check dist/autonomy.mjs",
    "start": "python3 -m http.server 8080 --directory dist"
  }
}
